{
    "name": "xlexp",
    "version": "3.0.0",
    "license": "MIT",
    "type": "module",
    "main": "src/index.js",
    "types": "types/index.d.ts",
    "scripts": {
        "dev": "esbuild src/index.js --outfile=dist/xlexp.min.js --bundle --charset=utf8 --format=esm --watch",
        "build": "esbuild src/index.js --outfile=dist/xlexp.min.js --bundle --minify --sourcemap --charset=utf8 --format=esm"
    },
    "dependencies": {
        "jszip": "^3.10.1"
    },
    "devDependencies": {
        "esbuild": "^0.25.0"
    }
}
